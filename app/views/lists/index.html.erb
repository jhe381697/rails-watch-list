<div class="container">
  <h1>Lists</h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li> <%# list_path(list) est une route parametrique %>
    <% end %>
  </ul>
  <%= link_to "Add a list", new_list_path %>
</div>